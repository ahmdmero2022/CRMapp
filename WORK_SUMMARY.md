# ملخص الأعمال المنجزة — CRM App

توثيق شامل لكل ما تم بناؤه وإنجازه في هذا المشروع.

## 1. نظرة عامة على المشروع

تطبيق CRM (إدارة علاقات العملاء) متكامل، مبني بالكامل باستخدام **Flutter و Dart**:

- **الواجهة الأمامية**: تطبيق Flutter ويب (Material 3) مع Riverpod لإدارة الحالة و go_router للتنقل.
- **الخادم (Backend)**: REST API بلغة Dart باستخدام `shelf` + `shelf_router`.
- **قاعدة البيانات**: SQLite (ملف واحد، بدون إعداد خارجي).

بنية المشروع:

```
CRMapp/
├── backend/    Dart REST API (shelf + shelf_router + sqlite3)
└── frontend/   Flutter web app (Riverpod + go_router + fl_chart)
```

## 2. الميزات المُنفَّذة

- **المصادقة (Authentication)** — تسجيل/دخول، تجزئة كلمات مرور مملّحة (HMAC-SHA256، 20 ألف تكرار)، رموز جلسة موقّعة (بدون الاعتماد على مكتبة JWT خارجية).
- **لوحة التحكم (Dashboard)** — إحصائيات حيّة، مخطط أعمدة لمراحل خط الأنابيب، مخطط دائري لحالة العملاء المحتملين، تغذية بآخر الأنشطة، والمهام القادمة.
- **جهات الاتصال (Contacts)** — إنشاء/تعديل/حذف، بحث، ربط بالشركات، سجل زمني للأنشطة والمهام والصفقات المرتبطة.
- **الشركات (Companies)** — إنشاء/تعديل/حذف، بحث، جهات اتصال وصفقات مرتبطة مع عدّادات مجمّعة.
- **العملاء المحتملون (Leads)** — إنشاء/تعديل/حذف، خط حالة (جديد → تم التواصل → مؤهل/غير مؤهل → تم التحويل)، وتحويل بضغطة واحدة إلى جهة اتصال + صفقة.
- **خط أنابيب الصفقات (Deals Pipeline)** — لوحة Kanban بالسحب والإفلات بين المراحل، إجماليات لكل مرحلة، تفاصيل الصفقة مع المهام والأنشطة.
- **المهام (Tasks)** — تواريخ استحقاق، أولوية، تبديل حالة الإنجاز، قابلة للربط بأي جهة اتصال/شركة/عميل محتمل/صفقة.
- **السجل الزمني للأنشطة (Activity Timeline)** — ملاحظات وأحداث تغيير حالة مسجّلة تلقائيًا لكل سجل.
- **الإعدادات والتوطين (Settings & Localization)** — دعم اللغتين (عربي/إنجليزي)، وضع فاتح/داكن، خطوط مدمجة محليًا (Roboto / NotoSansArabic) بدون الاعتماد على Google Fonts CDN لضمان عمل التطبيق دون اتصال بالإنترنت.

## 3. البنية التقنية

### الخادم (`backend/`)

- `db/database.dart` — اتصال SQLite واحد، هجرات `CREATE TABLE IF NOT EXISTS` قابلة لإعادة التشغيل، بذر مراحل خط الأنابيب الافتراضية الست.
- `auth/auth_service.dart` — تجزئة كلمات المرور المملّحة والممتدة، إصدار/التحقق من رموز الجلسة الموقّعة.
- `middleware/auth_middleware.dart` — التحقق من رأس `Authorization: Bearer`، مطبّق على كل مسارات `/api/*` ما عدا `/api/auth/*` و `/api/health`.
- `handlers/*_handler.dart` — معالج واحد لكل مورد (companies, contacts, leads, deals, tasks, activities, pipeline_stages, dashboard, auth) عبر SQL خام بمعاملات مُرَبَّطة (بدون ORM).
- `models/models.dart` — نموذج واحد لكل جدول، مع `fromRow`/`toJson`.
- معرّفات UUIDv4، طوابع زمنية ISO8601.

### الواجهة الأمامية (`frontend/`)

- `core/api/api_client.dart` — غلاف REST رفيع فوق حزمة `http`.
- `core/providers/repositories.dart` — طبقة مستودعات (Repository) لكل مورد.
- `core/providers/list_notifier.dart` — أساس مشترك لوحدات تحكم شاشات القوائم.
- تحديثات متفائلة (Optimistic Updates) لتفاعلات حسّاسة للزمن مثل سحب/إفلات الصفقات وتبديل إنجاز المهام.
- `core/router/app_router.dart` — توجيه واحد عبر `go_router` مع حماية بالمصادقة.
- بنية `features/<resource>/` متّسقة: شاشة قائمة + شاشة تفاصيل + نافذة نموذج لكل مورد.

## 4. البنية التحتية والتشغيل (DevOps)

- **GitHub Actions CI** (`.github/workflows/ci.yml`) — مهمّة للخادم (`dart analyze`) ومهمّة للواجهة (`flutter analyze` + `flutter build web --release --web-renderer html`) على كل push وpull request.
- **GitHub Codespaces** (`.devcontainer/devcontainer.json`) — صورة `ghcr.io/cirruslabs/flutter:3.24.5` جاهزة، تُثبّت الاعتماديات وتبني الواجهة وتشغّل الخادم تلقائيًا، مع إعادة توجيه المنفذ `8080` — تشغيل التطبيق كاملًا من المتصفح دون إعداد محلي.
- استخدام `--web-renderer html` بدلًا من CanvasKit لتفادي الاعتماد على CDN خارجي وضمان عمل التطبيق في بيئات معزولة عن الشبكة.

## 5. مسار التطوير (Pull Requests الرئيسية)

| PR | الوصف | الحالة |
|----|-------|--------|
| #1 | الإصدار الأول الكامل: واجهة Flutter + REST API + قاعدة بيانات | تم الدمج |
| #4 | إصلاحات مراجعة الكود: باگ مسح القيم الفارغة، إزالة سرّ JWT الثابت، جعل CORS قابلًا للتهيئة، إضافة CLAUDE.md | تم الدمج |
| #5 | إضافة التوطين والإعدادات والسمات (Theming)، مزامنة إعدادات CI/Codespaces | تم الدمج |
| #6 | إصلاح: استعادة صورة Flutter المفقودة في `devcontainer.json` بعد تعارض دمج | تم الدمج |

كل الملاحظات الآلية من **Devin Review** التي وردت على هذه الطلبات تمت معالجتها (أو تم التحقق من عدم وجود مشاكل).

## 6. الحالة الحالية

- كل الأعمال مدموجة في الفرع الرئيسي `main`.
- خطوط CI الخلفية والأمامية خضراء (ناجحة).
- التطبيق قابل للتشغيل الفوري عبر GitHub Codespaces دون أي إعداد محلي.
- لا يوجد حساب تجريبي مبدئي — يجب التسجيل من داخل التطبيق؛ جميع المستخدمين يشتركون في مساحة عمل واحدة (workspace) حاليًا.
