name: backend
description: CRM backend REST API server (Dart + shelf + SQLite).
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.5.4

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_static: ^1.1.2
  sqlite3: ^2.4.6
  crypto: ^3.0.3
  uuid: ^4.4.2

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
